# Core
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0
pillow>=10.0.0

# Detection & Tracking
ultralytics>=8.0.0             # YOLOv8 person detection
lap>=0.4.0                     # ByteTrack dependency (linear assignment)
filterpy>=1.4.5                # Kalman filter for tracking

# Person Re-ID
torchreid>=0.2.5               # OSNet and other Re-ID models
gdown>=5.0.0                   # torchreid dependency (model download)
tensorboard>=2.0.0             # torchreid dependency

# Face Recognition
insightface>=0.7.3             # Face detection + recognition
onnxruntime>=1.16.0            # ONNX inference (use onnxruntime-gpu for GPU)

# Database
sqlalchemy>=2.0.0

# API Server
fastapi>=0.100.0
uvicorn>=0.23.0

# Home Assistant Integration
paho-mqtt>=1.6.0

# CLI
click>=8.0.0

# PyTorch (for Re-ID models)
torch>=2.0.0
torchvision>=0.15.0

# Development
pytest>=7.0.0
